> data(mtcars)
> head(mtcars)
> model <- lm(mpg ~ wt, data = mtcars)
> summary(model) > plot(mtcars$wt, mtcars$mpg, main = "Simple Linear Regression: MPG vs Weight",xlab = "Weight (1000 lbs)",ylab = "Miles per Gallon", pch = 19)
> abline(model, col = "red", lwd = 2)
